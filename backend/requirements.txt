fastapi==0.111.1
python-dotenv==1.0.0
requests==2.32.0

# MongoDB async driver
# Password hashing
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Optional: email sending (for forgot password in future)
email-validator==2.0.0
