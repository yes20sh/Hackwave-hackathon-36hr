fastapi==0.111.1
uvicorn[standard]   # ASGI server for running FastAPI
python-dotenv==1.0.0
requests==2.32.0
motor==3.5.1        # MongoDB async driver
bson==0.5.10        # BSON utilities (sometimes included in pymongo, but safe to add)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.0.0
serpapi==0.1.5      # Google Search API client
pydantic==2.8.2     # (FastAPI requires Pydantic)

# pip install -r requirements.txt
